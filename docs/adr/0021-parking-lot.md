# Parking Lot: Open Items

A single running list of every open/unresolved item surfaced across AIARAP's ADRs, schema docs, and build plan — otherwise scattered as inline "open question"/"forward reference" notes that are easy to lose track of. **This doc is maintained continuously, not written once**: a new open item found anywhere else gets a row added here (in addition to wherever it was originally noted), and when an item is resolved its row is updated in place — status flips to Resolved, a resolution note + pointer to where the decision landed is added, and the source doc's own inline note is updated too. Rows are never deleted, so this stays a full history of what was deferred and why, not just a live TODO.

| # | Item | Raised in | Status | Resolution |
|---|---|---|---|---|
| 1 | Cross-currency threshold comparison: `payer_card_payment_policy.max_amount_currency` vs. the Invoice's actual currency — no FX conversion designed, currently just skips the charge on mismatch | [ADR-0019](0019-automatic-card-payment-batch.md) | Resolved | Converts using today's rate from `currency_exchange_rate` before comparing. An Invoice that still fails the threshold (or has no exchange rate available) is tracked in new `card_payment_threshold_exceeded` and alerted daily to the AR Clerk via a new Card Payment Threshold Alert job — see `0001-phase-1-table-structures.md` and `0002-scheduled-jobs.md`. |
| 2 | Exact SAP write-back retry count and backoff schedule for the Automatic Card Payment batch | [ADR-0019](0019-automatic-card-payment-batch.md) | Resolved | 3 retries within the same batch run, exponential backoff (30s, 2min, 10min) — sized for a transient SAP blip, not a cross-day retry, since money is already collected. |
| 3 | Fallback notification recipient when `payer_company_code.accounting_clerk_user_id` is NULL (no AR Clerk assigned) — intended fallback is that Tenant's Admin, but the lookup depends on the undrafted Security & Roles domain (`user_role`/`role`) | [ADR-0019](0019-automatic-card-payment-batch.md), [ADR-0020](0020-card-expiry-alert-and-stripe-sync.md) | Resolved | A minimal `role` + `user_role` slice (Parent Roles only, seeded per ADR-0010 — not the full Authorization Object/Permission/Derived Role domain) was pulled forward into the Tenancy & Identity domain specifically to unblock this. Fallback is now a real query: `SELECT app_user_id FROM user_role JOIN role ON role.id = user_role.role_id WHERE role.code = 'tenant_admin'`. See `0001-phase-1-table-structures.md`. |
| 4 | Exact Stripe webhook event name/payload for the card-data sync (Network Tokens/CAU update event) — needs verifying against the current Stripe API at implementation time | [ADR-0020](0020-card-expiry-alert-and-stripe-sync.md) | Resolved | Verified via Stripe's current docs: `payment_method.automatically_updated` (not the legacy `customer.source.updated`, which is Sources-API-only). |
| 5 | Stripe Connect onboarding flow itself — how/when a Tenant's connected account is created and `stripe_connected_account_id` (both copies) gets populated | [ADR-0020](0020-card-expiry-alert-and-stripe-sync.md) | Resolved | Stripe Standard connected account via Stripe Connect OAuth (Tenant owns their own Stripe relationship/compliance); flow documented in ADR-0020. |
| 6 | Credential Expiry Alert job's recipient lookup ("that Tenant's Admin(s)") is a forward reference to the undrafted Security & Roles domain (`user_role`, `role`) — same underlying blocker as #3, distinct job | [0002-scheduled-jobs.md](../schema/0002-scheduled-jobs.md) | Resolved | Same fix as item 3 — the minimal `role`/`user_role` slice resolves this job's recipient lookup too. |
| 7 | Credential Expiry Alert: should the job skip the check entirely when `*_credential_validity_days IS NULL`, rather than alerting? Presumed yes, not confirmed | [0002-scheduled-jobs.md](../schema/0002-scheduled-jobs.md) | Open | — |
| 8 | `card_payment`/`card_payment_attempt`.`invoice_id` is a forward reference with no FK constraint — add `REFERENCES invoice(id)` once the Invoice table exists (Core AR/AP domain, not yet designed) | [0001-phase-1-table-structures.md](../schema/0001-phase-1-table-structures.md) | Resolved | `invoice` table designed (Core AR/AP domain, AR side first per item 11). Both columns now carry a real `REFERENCES invoice(id)` FK. |
| 9 | `user_role` join table deferred — depends on `role`/`permission`/`authorization_object`, not yet drafted (Security & Roles domain, next up after Tenancy & Identity) | [0001-phase-1-table-structures.md](../schema/0001-phase-1-table-structures.md) | Open (narrowed) | `role` + `user_role` now exist as a minimal slice (Parent Roles only, to unblock items 3/6). Still open: `authorization_object`, `permission`, `role_authorization_value`, and Derived Role creation — the rest of the full Security & Roles domain remains deferred behind Core AR/AP. |
| 10 | Customer Representative assignment **revocation** mechanism (a Tenant Admin ending an already-approved assignment) | [ADR-0018](0018-customer-representative-assignment-tenant-approval.md) | Resolved | Confirmed: `assigned_customer_representative.is_active`/`deactivated_by`/`deactivated_at`/`deactivation_reason` (already in `0001-phase-1-table-structures.md`) is the mechanism — a permissioned Tenant Admin directly flips `is_active := false`, no separate revocation-request table, same pattern as ADR-0014's Payer/Vendor offboarding. ADR-0018 updated with a "Revocation (resolved)" section replacing its old "Open detail." |
| 11 | Confirm the AR-before-AP milestone ordering assumption | [0001-phase-1-build-plan.md](../plan/0001-phase-1-build-plan.md) | Superseded | Originally resolved as "AR first, AP after, both still Phase 1." [ADR-0035](0035-ap-functionality-moved-to-phase-2.md) supersedes this: AP is no longer Phase 1 scope at all, so the ordering question this row answered no longer applies — there's nothing left to sequence AP against within Phase 1. |
| 12 | Exact SAP-side technical mechanism a Tenant would use to call the SAP Payment Webhook — some older SAP ECC systems have no native outbound REST capability without middleware, which may mean this webhook isn't usable by every Tenant | [ADR-0022](0022-sap-payment-webhook.md) | Resolved | AIARAP builds and ships its own custom SAP program/package (deployed via transport onto each Tenant's system) rather than each Tenant building custom integration — narrows the risk to just "does this SAP system have any outbound HTTP client capability at all." Invoice sync remains the fallback for the residual case. Packaging/deployment mechanics of the package itself are a new, separate open item (22). |
| 13 | SAP Payment Webhook authentication mechanism specifics (bearer token vs. HMAC signature) | [ADR-0022](0022-sap-payment-webhook.md) | Resolved | Bearer token — matches native SAP HTTP destination/CPI capability without custom signing logic, consistent with item 12's resolution favoring broad Tenant compatibility. |
| 14 | Exact width of the Stripe reconciliation rolling grace-period window before flagging `missing_in_stripe_extract` (must exceed normal payout settlement delay + UTC-cutoff margin) | [ADR-0025](0025-stripe-payout-reconciliation.md) | Resolved | 5 business days — covers Stripe's standard 2-day payout settlement plus margin for newer/slower accounts and the UTC-cutoff effect. Not yet configurable per-Tenant/region if a specific Tenant's schedule is slower. |
| 15 | Stripe Payout Reconciliation job tier and cadence — not committed | [ADR-0025](0025-stripe-payout-reconciliation.md) | Resolved | NestJS, daily — same category as AR Reconciliation (no money movement/restart-safety stakes); daily is safe even for Tenants on a slower Stripe payout schedule. |
| 16 | The Notification Channel/Email delivery mechanism itself has never been designed — referenced by name in nearly every job this session (Credential Expiry Alert, Card Expiry Alert, AR Clerk notifications on SAP write-back failure) but there's no table/mechanism for how a Notification is actually composed, queued, sent, or retried | [ADR-0026](0026-notification-channel.md) | Resolved | `notification_template` + `notification` (send log) + `notification_sender_identity` (Tenant-registered sender addresses) designed — see `0001-phase-1-table-structures.md`. |
| 17 | Notification retry mechanism for a genuinely failed send (transient provider error) — `notification.retry_count` exists for tracking, no retry job/backoff schedule designed | [ADR-0026](0026-notification-channel.md) | Open | — |
| 18 | Email provider specifics (AWS SES vs. another provider) not decided | [ADR-0026](0026-notification-channel.md) | Open | — |
| 19 | Sender identity verification: domain-level design resolved (Tenant supplies only the domain; SES-generated DKIM/SPF DNS records tracked per-record in `notification_sender_dns_record`) — still open: the mechanics of *how* AIARAP learns a record verified (SES→SNS webhook vs. polling job) | [ADR-0026](0026-notification-channel.md) | Open (narrowed) | Domain-level model + `notification_sender_dns_record` designed in `0001-phase-1-table-structures.md`; the actual verification-confirmation mechanism remains open. |
| 20 | Whether the SPF TXT record is required or merely recommended, and how to handle a Tenant who already has an existing SPF record at their domain (merge into one record vs. requiring them to do it themselves) | [ADR-0026](0026-notification-channel.md) | Open | — |
| 21 | Exact Stripe OAuth scopes requested during Stripe Connect onboarding, and how a Tenant later *disconnects* Stripe (reversing `stripe_enabled`) | [ADR-0020](0020-card-expiry-alert-and-stripe-sync.md) | Resolved | Scope: `read_write` (the only option supporting charge creation/card registration). Disconnect: formal OAuth deauthorize call, `stripe_disconnected_at` set (new column), `stripe_connected_account_id` kept as historical record (not cleared), every `payer_payment_card` bulk-deactivated. See ADR-0020 and `0001-phase-1-table-structures.md`. |
| 22 | Packaging/deployment mechanics of AIARAP's custom SAP program for the SAP Payment Webhook (transport request format, versioning/update process across Tenants, detection trigger — a BTE/exit on FI posting vs. a periodic in-SAP job) | [ADR-0022](0022-sap-payment-webhook.md) | Resolved | AIARAP-registered SAP namespace; BTE-based real-time trigger (exact event ID left for implementation-time ABAP verification, version-dependent); two-package model — uniform base package (one build, many deployments) + bespoke customer-specific package per Tenant, connected via a BAdI/customer-exit extension point in the base package, not two independent transports; transport-based delivery through each Tenant's own change management (AIARAP can't centrally push, unlike Salesforce); both package versions self-reported on every webhook push. See ADR-0022. |
| 23 | Whether the Stripe reconciliation grace-period window (5 business days, item 14) needs to become Tenant/region-configurable rather than a single platform-wide constant, for Tenants on a slower Stripe payout schedule | [ADR-0025](0025-stripe-payout-reconciliation.md) | Resolved | Auto-derived per Tenant from Stripe's own Account API (`settings.payouts.schedule`), synced every run into `tenant_settings.stripe_payout_interval`/`stripe_payout_delay_days` — not a manual override. Grace period = delay + a small safety margin (exact margin width still a minor open item within ADR-0025), falling back to the 5-day platform default only before first sync. |
| 24 | Exact set of event types AIARAP's Salesforce AppExchange package will actually send — the generic mechanism is designed, not the specific integrations built on it | [ADR-0027](0027-salesforce-appexchange-integration.md) | Open | — |
| 25 | Public "Listed" vs. private "Unlisted" AppExchange package distribution — a go-to-market decision, not a schema one | [ADR-0027](0027-salesforce-appexchange-integration.md) | Open | — |
| 26 | Salesforce package versioning/upgrade process across installed Tenant orgs, and how each event type's Salesforce-side detection trigger is implemented (Platform Event, Apex trigger, scheduled Apex job) | [ADR-0027](0027-salesforce-appexchange-integration.md) | Open | — |
| 27 | How AR Reconciliation merges real-time webhook-pushed balance data (from the SAP program / Salesforce package, per the "critical jobs carry richer payloads" principle) with the periodic batch extraction when both are available for the same account — which one wins, or how they're reconciled against each other | [ADR-0023](0023-ar-reconciliation-batch.md) | Resolved | No actual merge needed — a scheduled run always wins on freshness by construction (live SAP pull at run time). New `ar_reconciliation_run.trigger_type` (`'scheduled'` \| `'webhook_triggered'`) lets a webhook push instead fire its own single-account run immediately, narrowing the detection window between scheduled sweeps rather than competing with them. Both write to the same account/discrepancy tables as independent, coexisting rows. See ADR-0023 and `0001-phase-1-table-structures.md`. |
| 28 | Whether a `webhook_triggered` AR Reconciliation run finding a discrepancy should fire its own immediate Notification (reusing the Notification Channel, ADR-0026), or simply wait to be surfaced on the next scheduled dashboard view | [ADR-0023](0023-ar-reconciliation-batch.md) | Resolved | Yes — `webhook_triggered` runs notify the AR Clerk immediately (Notification Channel); scheduled runs stay dashboard-only, not retroactively changed. Reasoning: it's the reason the fast path exists, and a webhook-triggered finding is a fresher, more actionable, single-account event than a routine scheduled result. Caveat: no dedup/`resolved_at` tracking on `ar_reconciliation_discrepancy`, so repeat notifications are possible before a fix lands — accepted as a minor Phase 1 tradeoff. See ADR-0023. |
| 29 | Exact default seed mapping of every current Stripe `balance_transaction.type` value to a bucket in `stripe_transaction_type_bucket` — the mechanism is designed, not the specific seed data | [ADR-0025](0025-stripe-payout-reconciliation.md) | Resolved | Full mapping table compiled and documented in ADR-0025 (8 buckets, each with its reasoning). Along the way, the table also gained `debit_gl_account`/`credit_gl_account` (reference-only, not automated posting — that stays Out of Scope) + `debit_posts_to_customer`/`credit_posts_to_customer` flags (real AR double-entry logic — a charge credits the Customer, a refund debits it), and moved to Company-Code grain (`stripe_payout`/`stripe_payout_transaction` both gained `company_code`, matching everything else that moved once Stripe Connect did). Not authoritative forever — Stripe adds new types over time; the table's own FK makes an unmapped new type fail loudly rather than silently miscategorize. |
| 30 | `var.tenant_sap_secret_arn_pattern` (Java's outbound Secrets Manager IAM policy, `terraform/shared/java_outbound.tf`) is a guessed resource pattern for Tenant SAP credential secrets — not confirmed against the app's actual secret-creation code | [docs/infra/README.md](../infra/README.md) | Open | — |
| 31 | Java's actual outbound network egress isn't provisioned — only Secrets Manager read + SQS publish access exist. A NAT Gateway route for java-app's subnet, and a Transit Gateway VPN attachment for any Tenant requiring private connectivity instead of public HTTPS/OAuth, are both still needed | [terraform/shared/README.md](../../terraform/shared/README.md) | Open | — |
| 32 | `java_outbound_policy_arn` isn't automatically attached to java-app's real IAM instance role — only happens if `java_app_iam_role_name` is explicitly set; otherwise it's just a Terraform output to attach manually | [terraform/shared/README.md](../../terraform/shared/README.md) | Open | — |
| 33 | Whether `global.tenant_registry` is actually deployed yet in the real `aiarap` database — `pg-inventory-writer`'s migration deliberately has no FK into it (to avoid a migration-ordering dependency between two separately-deployed projects), but that also means nothing currently confirms both projects' schemas actually coexist cleanly in practice, not just on paper | [docs/infra/README.md](../infra/README.md) | Open | — |
| 34 | Node prod (a dedicated EC2 instance) and SAP prod (a second HANA instance) for the Tenant integration hub aren't provisioned yet — `terraform/shared` currently carries a placeholder instance ID for node prod, guarded by a `check` block that warns until it's replaced | [INFRASTRUCTURE_REFERENCE.md](../infra/INFRASTRUCTURE_REFERENCE.md) | Open | — |
| 35 | Several account-specific Terraform variables for the Tenant integration hub still need real values before first apply: private subnet IDs, the SAP tailnet-proxy EC2 instance ID, the Route53 zone ID for `varunerpsolutions.com`, and the `aiarap` RDS instance identifier + Secrets Manager secret ARN for the inventory writer | [terraform/shared/README.md](../../terraform/shared/README.md) | Open | — |
| 36 | The SAP tailnet-proxy's own nginx/socat forwarding config (each environment's NLB target port → the matching SAP HANA instance's Tailscale address) isn't built — `var.sap_environments`'s `port` values are just NLB target ports; something on the proxy box itself still has to actually forward them | [terraform/shared/README.md](../../terraform/shared/README.md) | Open | — |
| 37 | Exact SAP API/BAPI for the live pricing/tax simulation call in the Sales Order checkout flow — now routed through Java per ADR-0031's BAPI-first preference (e.g. `BAPI_PRICES_CONDITIONS` on ECC, OData pricing-simulation service as the S/4HANA fallback if no released BAPI covers it) | [ADR-0029](0029-sales-order-checkout-fi-down-payment.md), [ADR-0031](0031-sap-integration-bapi-first-java-owns-all-sap-calls.md) | Resolved | `BAPI_SALESORDER_SIMULATE` — a dedicated dry-run BAPI (pricing, availability, schedule lines) that creates no document, the correct fit for this step specifically (unlike `BAPI_SALESORDER_CREATEFROMDAT2`, item 38, which redetermines pricing only as a side effect of actually creating the order). OData/S/4HANA fallback still unverified. |
| 38 | Exact SAP API/BAPI for real-time Sales Order creation — now routed through Java per ADR-0031's BAPI-first preference (e.g. `BAPI_SALESORDER_CREATEFROMDAT2`, the S/4HANA `API_SALES_ORDER_SRV` OData service as fallback) | [ADR-0029](0029-sales-order-checkout-fi-down-payment.md), [ADR-0031](0031-sap-integration-bapi-first-java-owns-all-sap-calls.md) | Resolved | `BAPI_SALESORDER_CREATEFROMDAT2`. OData/S/4HANA fallback (`API_SALES_ORDER_SRV`) still unverified. |
| 39 | AR Reconciliation (ADR-0023) and AR Aging (ADR-0024) don't currently account for FI Down Payment postings — a Down Payment posts against a different reconciliation account (Advances from Customers, not Trade Receivables) and isn't a normal open-AR item until cleared. Whether a pending Down Payment should appear in either report, or be deliberately excluded until it clears into a real Invoice, is undecided | [ADR-0029](0029-sales-order-checkout-fi-down-payment.md) | Resolved | Visible in both, but never blended into the existing numbers. AR Reconciliation: new `ar_reconciliation_account.pending_down_payment_amount`, additive visibility only, never summed into `variance_amount`. AR Aging: new `ar_aging_snapshot_down_payment` table, same Payer/Company Code/currency grain as `ar_aging_snapshot_bucket`, deliberately not folded into any aging bucket (a Down Payment isn't an aged receivable, it's a credit already collected). Rationale: a Payer checking their AIARAP balance should see the true full picture. See ADR-0023 and ADR-0024. |
| 40 | Exact retry count/backoff schedule for the SAP order-creation failure path (card already charged, SAP Sales Order creation call fails) — likely mirrors ADR-0019's 3x/30s-2m-10m, not yet confirmed | [ADR-0029](0029-sales-order-checkout-fi-down-payment.md) | Resolved | Reuses ADR-0019's exact schedule — 3 retries, exponential backoff (30s, 2min, 10min) — since it's the same shape of situation (money already collected, SAP-side write needs a resilient retry before escalating to a human). Drives `sales_order.sap_creation_attempts`/`sap_creation_status`. |
| 41 | SAP Digital Access / indirect-use licensing exposure from the Sales Order checkout flow's live SAP calls (pricing simulation + real-time order creation): this is a **Tenant-side contractual matter** with SAP (each Tenant needs either sufficient Named User licenses or a Digital Access Adoption contract covering AIARAP-originated document creation), not something AIARAP's choice of API technology resolves — OData vs. BAPI makes no licensing difference (both are equally licensable if they create a Sales Order; the real distinction between them is supported/released vs. unsupported/internal interfaces, a stability concern, not a licensing one). Precedent: *SAP UK Ltd v Diageo Great Britain Ltd* [2017] EWHC 189 (TCC) — SAP won on liability for indirect use via a third-party front end (Salesforce + a bespoke ordering tool) creating SAP Sales Orders without Named User licenses, ~£54.5M in damages, the case that pushed SAP toward the 2018 Digital Access per-document model | [ADR-0030](0030-sap-digital-access-tenant-responsibility.md) | Resolved | Codified as a standard AIARAP customer contract clause: the Tenant must confirm adequate Digital Access/Named User coverage in writing from their own SAP rep before enabling any document-creating feature (starting with Sales Order checkout); AIARAP bears no liability. Contract clause only, no schema-level gate (unlike `company_code.down_payment_configured`, a technical prerequisite). Exact clause language remains a legal-drafting task, not specified in the ADR. |
| 42 | Whether Salesforce's own licensing model presents an analogous indirect-access risk to AIARAP (relevant to the Salesforce AppExchange Integration, ADR-0027) | [ADR-0027](0027-salesforce-appexchange-integration.md), [ADR-0030](0030-sap-digital-access-tenant-responsibility.md) | Resolved | No SAP-style indirect-access litigation risk — Salesforce access authenticates via a purpose-built Integration User License, and the real constraint is a technical daily API call limit (by edition/license count), not a contractual "indirect use" claim. Different mitigation than the SAP clause: AIARAP proactively provides the Tenant a written API call volume estimate during onboarding, before enabling any Salesforce-integrated feature, so a capacity shortfall isn't a surprise. See ADR-0030. Narrower open items remain — see that ADR's Open Items (exact estimate methodology, current Salesforce API limit formula/Integration User License terms not verified against live Salesforce docs). |
| 43 | Exact synchronous internal API mechanism between NestJS and the Java service for real-time SAP calls (REST vs. gRPC, internal auth scheme) — Java's scope just broadened from batch-only to also handling real-time calls | [ADR-0031](0031-sap-integration-bapi-first-java-owns-all-sap-calls.md) | Open | — |
| 44 | Latency budget/SLA for the checkout flow's call chain now that it's NestJS → Java → SAP → Java → NestJS instead of a direct NestJS-to-SAP call — not yet assessed against the interactive checkout UX | [ADR-0029](0029-sales-order-checkout-fi-down-payment.md), [ADR-0031](0031-sap-integration-bapi-first-java-owns-all-sap-calls.md) | Resolved | No SLA number set — not something to pin down speculatively. Depends on the actual Java↔SAP BAPI round-trip time and the NestJS↔Java internal call overhead (item 43), neither measurable yet. Deliberately deferred to implementation time: measure the real call chain's latency once built, then discuss the acceptable checkout-UX threshold with the Tenant — not a schema/design decision to resolve now. |
| 45 | Whether the Java service should split into two deployables (a batch runner + a separate synchronous API service) or stay one, now that it owns both nightly batch and real-time SAP calls | [ADR-0031](0031-sap-integration-bapi-first-java-owns-all-sap-calls.md) | Open | — |
| 46 | Exact "reasonable window" before the Sales Order Down Payment Clearing job's daily safety-net sweep treats a still-`pending` `sales_order_payment` row as worth re-checking, vs. simply waiting for the next natural event-driven trigger (Invoice extraction/sync) | [0002-scheduled-jobs.md](../schema/0002-scheduled-jobs.md) | Resolved | 24 hours since `charged_at`. The Nightly Bulk Extraction batch runs every 24h and Incremental Sync runs frequently throughout the day, so within any 24h window the event-driven path has almost certainly already had multiple chances to fire if the matching Invoice exists — a row still pending past that point is worth the sweep's attention; younger rows are left to the event path alone rather than re-checked daily for no reason. |
| 47 | Automatic Bank Debit Payment batch job (ACH/SEPA equivalent of the Automatic Card Payment batch, ADR-0019) not yet designed/added to `0002-scheduled-jobs.md` | [ADR-0032](0032-ach-sepa-bank-debit-payments.md) | Resolved | Added: Java + Spring Batch, mirrors the Automatic Card Payment batch's shape/restart-safety justification, checked against `payer_bank_account.self_imposed_limit_amount` instead of a policy table. Paired with a new Stripe Bank-Debit Webhook Sync job for the multi-day settlement/return outcome. See `0002-scheduled-jobs.md`. |
| 48 | Exact Stripe event names/payloads for ACH/SEPA settlement confirmation and return notification — not yet verified against Stripe's current API | [ADR-0032](0032-ach-sepa-bank-debit-payments.md) | Resolved | Verified against Stripe's own docs — turns out to be two different mechanisms. Return: `charge.dispute.created` (event-driven; Stripe explicitly documents this exact "late ACH failure after `succeeded`" scenario, reason codes `insufficient_funds`/`incorrect_account_details`/`bank_cannot_process`; SEPA equivalent is a customer dispute, 8-week no-questions-asked or 13-month unauthorized-claim window). Settlement: **no distinct webhook exists** — it's a documented time expectation only (ACH T+4 standard/T+2 faster, SEPA T+6), so `settlement_status = 'settled'` is set by a daily time-based sweep, not an event. A return can still arrive after `'settled'` (SEPA's 13-month window far outlasts its 6-day settlement window) — handled by the same return-event logic regardless of prior `settlement_status`. Stripe Bank-Debit Webhook Sync job updated accordingly (`0002-scheduled-jobs.md`). |
| 49 | Whether `payer_bank_account.self_imposed_limit_period = 'monthly'` uses a rolling window or calendar-month definition, and how a mid-period charge is evaluated against it | [ADR-0032](0032-ach-sepa-bank-debit-payments.md) | Resolved | Calendar month (resets the 1st), not a rolling window. Evaluated live at each charge attempt — sum the bank account's own successful (non-returned) `bank_debit_payment` amounts from the 1st of the current calendar month through now, skip the charge if adding it would exceed `self_imposed_limit_amount`. No separate tracking table (unlike `card_payment_threshold_exceeded`) — that exists for FX-conversion complexity this doesn't have; a failed check just logs as a normal `bank_debit_payment_attempt`. |
| 50 | Quantified fee comparison/business case for ACH/SEPA vs. card interchange that motivated pulling this into Phase 1 — a finance/go-to-market input, not yet captured anywhere | [ADR-0032](0032-ach-sepa-bank-debit-payments.md) | Resolved | Not AIARAP's to quantify — each Tenant's own fee economics (their actual negotiated card interchange rate vs. ACH/SEPA pricing) determine whether/how much they promote ACH/SEPA to their own Payers. AIARAP builds and offers the capability platform-wide; whether it's worth using for a given Tenant is that Tenant's own business decision, not a platform-wide business case AIARAP needs to establish. |
| 51 | Stripe Connect's original "one account per Tenant" design (ADR-0020) needed reopening once ACH/SEPA registration surfaced that a Tenant with multiple Company Codes (separate legal entities/bank accounts) needs independent payout settlement per one | [ADR-0020](0020-card-expiry-alert-and-stripe-sync.md), [ADR-0032](0032-ach-sepa-bank-debit-payments.md) | Resolved | Moved to one Stripe Connected Account per **Company Code**: `stripe_*` fields relocated from `tenant_settings` to `company_code`; `global.tenant_registry`'s single-column routing pointer replaced by `global.stripe_account_routing` (one row per account); `payer_payment_card`/`payer_bank_account` both gained a required `company_code` (a PaymentMethod token only exists within the one Connected Account it was registered under). **Note**: items 5, 15, 21, and 23 above describe the original per-Tenant design and are accurate only as a historical record of what was resolved at the time — read them alongside this item, not as still-current. |
| 52 | Sales Order checkout's optimistic settlement (SAP order created immediately on charge success) is riskier for ACH/SEPA than for a card, since fulfillment could begin before a multi-day-delayed charge is actually final | [ADR-0029](0029-sales-order-checkout-fi-down-payment.md), [ADR-0032](0032-ach-sepa-bank-debit-payments.md) | Resolved | New `company_code.bank_debit_order_confirmation_mode` — Tenant chooses per Company Code between `hold_in_aiarap` (defer SAP order creation until settlement confirms; new `sales_order.status` values `held_pending_settlement`/`held_payment_returned`) and `submit_with_delivery_block` (create immediately under a reason-coded SAP Delivery Block, cleared once settled). New Sales Order Bank-Debit Confirmation Batch job drives both paths. See ADR-0032 and `0002-scheduled-jobs.md`. |
| 53 | Whether the Sales Order Bank-Debit Confirmation Batch needs a periodic safety-net sweep (mirroring the Sales Order Down Payment Clearing job's daily sweep) in case an event-driven settlement/return notification is missed | [ADR-0032](0032-ach-sepa-bank-debit-payments.md), [0002-scheduled-jobs.md](../schema/0002-scheduled-jobs.md) | Resolved | Yes, but the risk it guards against is a **downstream reaction failure**, not a missed signal — `sales_order_payment.settlement_status` reaching `'settled'`/`'returned'` is already resilient by construction (settlement from a daily time-based sweep, return from `charge.dispute.created`), but that's a one-way transition; if this job's own reaction (release the held order, clear the delivery block) fails to fire right after, nothing re-tries it. Daily sweep checks for that specific inconsistency (`sales_order` still `held_pending_settlement`/still carrying `delivery_block_code` when `settlement_status` already resolved) and re-triggers the missed action. See `0002-scheduled-jobs.md`. |
| 54 | Exact SAP condition type(s)/table(s) to extract from for the new List Price Extraction job's two scopes (Sales-Area-level list price vs. Customer-specific override) — version/implementation-dependent | [ADR-0029](0029-sales-order-checkout-fi-down-payment.md), [0002-scheduled-jobs.md](../schema/0002-scheduled-jobs.md) | Open | — |
| 55 | Path-level authorization now that 3 scopes (`node.invoke`, `node.portal`, `node.support`) share one Node API stage — the Lambda authorizer's current policy returns a blanket allow on the whole stage once any required scope matches, which no longer isolates the three from each other | [ADR-0033](0033-public-portal-support-app-exposure.md) | Resolved | The Lambda authorizer accepts any of the 3 scopes (previously only `invoke`) and passes which one matched through as `context.purpose`, but still returns the same stage-wide `Allow` regardless — that's by design, not the fix. Actual per-route restriction now lives in `AIARAP-node-backend`'s `ScopeGuard` (`@RequireScope`, global `APP_GUARD`, fails closed on an unmarked route, independently re-verifies the token since API Gateway doesn't forward authorizer context into the HTTP_PROXY integration). Unit-tested + smoke-tested against a booted app. |
| 56 | CI/CD rework for `react-external-app`/`react-support-app`: today's pipeline builds a Docker image and deploys via SSM to the shared `react-app` EC2 instance; needs to become build-static-assets → S3 upload → CloudFront invalidation, per app per environment | [ADR-0033](0033-public-portal-support-app-exposure.md) | Open | The hosting target this pipeline would deploy to now exists (`modules/spa-hosting`, `terraform/shared/public_apps.tf` — 6 S3 buckets + CloudFront distributions, IDs exposed via `shared/outputs.tf`'s `portal_app_hosting`/`support_app_hosting`), but the pipeline itself isn't built. Unaffected by ADR-0034: the SPA build stays one artifact per environment either way — ADR-0034 only adds per-Tenant CloudFront *aliases* on top of the existing per-environment bucket/distribution, it doesn't multiply the build/upload step per Tenant. |
| 57 | Whether the `react-app` EC2 instance is decommissioned once all 3 environments serve from S3+CloudFront, or kept for a developer's local-network preview use case | [ADR-0033](0033-public-portal-support-app-exposure.md) | Open | — |
| 58 | Whether portal/support human login (Authorization Code+PKCE) belongs on the same Cognito user pool (`varunerp-integration-pool`) as the existing M2M `client_credentials` integration clients, or warrants a separate pool | [ADR-0033](0033-public-portal-support-app-exposure.md) | Superseded (for portal only) | Reopened by [ADR-0034](0034-per-tenant-portal-domain-and-cognito-pools.md), scoped to `react-external-app` only: 3 separate pools, one per environment (not the shared `varunerp-integration-pool`, not per-Tenant). `react-support-app` keeps this row's original resolution — still the shared pool, unchanged. |
| 59 | Self-registration (Cognito Hosted UI sign-up) for portal users vs. ADR-0014's Access-Request-only onboarding — genuinely conflicting, not reconciled | [ADR-0034](0034-per-tenant-portal-domain-and-cognito-pools.md), [ADR-0014](0014-tenant-managed-payer-vendor-user-onboarding-offboarding.md) | Open | — |
| 60 | Per-Tenant CloudFront alias/cert provisioning mechanism for the portal domain — extending `modules/tenant-onboarding` (or a new sibling module) to add a portal alias per Tenant per environment | [ADR-0034](0034-per-tenant-portal-domain-and-cognito-pools.md) | Open | — |
| 61 | Tenant-resolution mechanism within a per-environment-shared Cognito pool (proposed `custom:tenant_subdomain` attribute, set from registration hostname) — including what actually stops a token issued for one Tenant's subdomain from being presented against another Tenant's data | [ADR-0034](0034-per-tenant-portal-domain-and-cognito-pools.md) | Open | — |
| 62 | Whether Vendor-side Access Request/Admin bootstrap/User termination (ADR-0014/0015/0016, written generically as "Payer/Vendor") should also be excluded from Phase 1 now that AP is entirely Phase 2 — there's no Phase 1 feature for an onboarded Vendor User to actually use — or left in since the mechanics are shared with Payer at little extra cost | [ADR-0035](0035-ap-functionality-moved-to-phase-2.md) | Resolved | Also Phase 2 — no Phase 1 feature for a Vendor User to reach, so building the onboarding/admin flow now would mean a Vendor Admin logs into an empty portal. ADR-0014/0015/0016 unchanged in substance (Payer-only for Phase 1, applies to Vendor once AP ships) — scope notes added to all three. Build plan: `Vendor` dropped from Milestone 0's Phase 1 entity list; Milestone 1's Access Request/Admin flows scoped to Payer only; Vendor-side flows added to the Phase 2 section alongside the rest of AP. Spec: Access model/termination/onboarding-offboarding bullets marked Payer-only for Phase 1; new Out of Scope bullet. See ADR-0035. |

## How to use this doc

- **Adding an item**: when a new open question/forward-reference surfaces while working on something else, note it inline in that doc (as has been the convention) *and* add a row here in the same edit pass — don't let it exist in only one place.
- **Resolving an item**: when a row's question gets answered, update its **Status** to `Resolved`, fill in **Resolution** with a one-line summary + a link to wherever the decision was actually recorded (an ADR section, a table's new column, a spec line), and go back and update the *source* doc's own inline note so it no longer reads as open. Do this in the same pass as making the decision, consistent with this project's established "write decisions immediately" working convention.
- **Never delete a row** — a resolved item stays as a record of what was once open and how it got closed.
